version: '3.9'

services:
  database:
    image: postgres:15.3-bookworm
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: todo
      POSTGRES_USER: todo
      POSTGRES_PASSWORD: secret
